# Análisis del reto (unidad 5)
*Realizado por Juan Alejandro Solórzano*
## Introducción:
> Para la realización del reto de la unidad 5, se hizo uso de todos los conceptos y temas vistos en clase (además de otros temas que no alcanzaron a explicarse, como el modulo 'os'). Inclusive, los requerimentos del reto hicieron necesario el uso de temas vistos anteriormente (como los vistos en el tema de estructuras de datos).

## Elaboración del programa:
> Se inicia comprendiendo cómo será/debe ser el funcionamiento del programa, de acuerdo a las instrucciones dadas por el docente. Seguidamente, se realiza una investigación de las funciones y métodos en Python adecuadas para usarse en el programa finalizado, para así asegurar que el programa funcione correctamente. 

> Tras realizar esta investigación, se inicia la construcción del código. Primero, se realiza el menú de procesamiento de archivos tipo .txt. Posteriormente se realiza el menú de procesamiento de archivos .csv, y se finaliza con la opción de listado de archivos en carpetas (opción 1 en el menú principal.)

> Finalmente, tras haberse elaborado, se realizan varias ejecuciones del programa para verificar que este funcione adecuamente, y se realizan correcciones cuando se es necesario.

## Uso de conceptos estudiados durante la unidad:
> Como se mencionó anteriormente, se hizo uso extensivo de los conceptos vistos en clase. Entre estos conceptos, se incluyen:

1. ``Apertura de archivos (funciones with() y open()):`` concepto imprescindible para realizar todas las tareas y funciones especificadas en el reto.
2. ``Lectura y escritura de archivos (modos "r", "w"):`` permite especificarle al programa el modo de apertura, y así realizar las tareas siguientes (como analizar caracteres, reemplazar palabras, etc.)
3. ``Métodos de lectura de archivos:`` fueron esenciales para realizar todas las tareas pedidas para el programa, pues, para cada menú y opción, (en este caso) siempre se empieza por leer el archivo, y tras esto se continúa con las demás tareas a realizar.
4. ``Libreria matplotlib y sus métodos:`` concepto súmamente importante para algunas opciones de procesamiento de archivos .txt y .csv, pues en estas se pedía que el programa elaborase gráficas de acuerdo con ciertos datos. Dichas gráficas son posibles gracias a la libreria 'matplotlib' y sus métodos asociados.
5. ``Manejo de archivos .csv:`` se destaca este concepto pues, en lo personal, el manejo de archivos .csv (dentro del código) fue una tarea un poco compleja, la cual se pudo lograr gracias a los conceptos enseñados sobre el manejo de este tipo de archivos.